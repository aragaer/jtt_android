<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aragaer.jtt"
    android:versionCode="9"
    android:versionName="1.3.2" >

    <uses-sdk android:minSdkVersion="4"
        android:targetSdkVersion="9"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".JTTActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".JTTAlarmActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".JTTSettingsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".JTTMainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/JTTTheme" >
            <intent-filter android:label="@string/abbr_app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".JTTService"
            android:exported="false"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.aragaer.jtt.JTTService" />
            </intent-filter>
        </service>

        <receiver android:name=".JTTService$JTTStartupReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>
    </application>

</manifest>